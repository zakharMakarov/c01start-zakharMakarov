#include <iostream>
using namespace std;

int main()
{
	int x;
	int p;
	cout << "Введите x " << endl;
	cin >> x;
	2 * (x * x * x * x) - 4 * (x * x * x) + (x * x) + 3 * x + 2;
	cout << "Ответ: " << p << endl;
	return 0;
}